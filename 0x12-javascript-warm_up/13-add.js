#!/usr/bin/node

exports.add = function (a, b) {
  return (a + b);
};

/*
let a = Math.floor(process.argv[2]);
let b = Math.floor(process.argv[3]);
if (isNaN(a) || isNaN(b)) {
  console.log('NaN');
} else {
  console.log(add(a, b));
}
*/
