-- MySQL script to list databases on this server
-- if SQL not started, script can be run like so:
-- cat 0-list_databases.sql | mysql -hlocalhost -uroot -p
-- a command I like: sudo service mysql start
-- another command:  mysql -u root -proot --execute="SHOW DATABASES"
SHOW DATABASES;
